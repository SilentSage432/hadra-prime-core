# DC-09 — ADRAE Dedicated Disk Mount
machine:
  disks:
    - device: /dev/sdb
      partitions:
        - mountpoint: /var/mnt/ADRAE
          size: 100%
          filesystem:
            type: ext4

